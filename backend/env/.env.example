# Runtime Environment
NODE_ENV=development

# Server Configuration
PORT=3000

# CORS Configuration (sensitive - deployment specific)
# Comma-separated list of allowed CORS origins
CORS_ALLOWED_ORIGINS="http://localhost:5173,http://localhost:5174"

# Logging Configuration
LOG_LEVEL=debug
LOG_FORMAT=pretty

# MediaSoup Configuration (sensitive - public IP)
# For development: use your local IP (get with: ip route get 8.8.8.8 | awk '{print $7}' | head -1)
# For production: use your server's public IP address
# Examples:
#   Local development: 127.0.0.1
#   LAN development: 192.168.x.x
#   Production: your.public.ip.address
MEDIASOUP_ANNOUNCED_IP=127.0.0.1 